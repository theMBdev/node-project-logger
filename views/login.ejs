<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <title>title</title>
        <link rel="stylesheet" href="/css/stylesheet.css" type="text/css">

        <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700&&family=Noto+Sans&display=swap" rel="stylesheet">

    </head>
    <body>

        <main class="page-container">

            <div class="form-card">        

                <h1>Login</h1>

                <%- include ('./partials/messages') %>

                <form action="/user/login" method="POST">

                    <div class="">
                        <input
                               type="email"
                               id="email"
                               name="email"
                               class="input input--full-width"
                               placeholder="Enter Email"
                               />
                    </div>
                    <div class="">
                        <input
                               type="password"
                               id="password"
                               name="password"
                               class="input input--full-width"
                               placeholder="Enter Password"
                               />
                    </div>
                    <button type="submit" class="button-primary button-primary--full-width ">Login</button>
                </form>
                <div class="form-card__switch">
                    <p class="form-card__text">Don't have an account</p>             
                    <a class="form-card__link" href="/user/register">Register</a>
                </div>

            </div>
        </main>
    </body>
</html>